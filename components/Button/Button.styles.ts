import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    color: "white",
    paddingHorizontal: 20,
    paddingVertical: 16,
    borderRadius: 8,
    textAlign: "center",
    width: 200,
    alignSelf: "center",
  },
});

export { styles };
