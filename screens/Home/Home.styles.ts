import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  greetings: {
    flex: 1,
  },
  recentMoods: {
    marginTop: 'auto',
    marginBottom: 'auto',
  },
  actions: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export { styles as homeStyles };
